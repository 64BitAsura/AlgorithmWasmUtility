{
  "name": "wasmAlgorithms",
  "version": "1.0.0",
  "engines": {
    "node": "16.x"
  },
  "description": "test assemblyscript",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "asbuild:untouched": "asc ./graph/**/*.ts --target debug --config ./graph/**/asconfig.json",
    "asbuild:optimized": "asc ./graph/**/*.ts --target release --config ./graph/**/asconfig.json",
    "asbuild": "npm run asbuild:untouched && npm run asbuild:optimized"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@assemblyscript/loader": "^0.19.23"
  },
  "devDependencies": {
    "assemblyscript": "^0.19.23",
    "jest": "latest"
  }
}
